// App.xaml.cs — Code-behind của App.xaml (chỉ cần minimal)
namespace VideoLocalizer;

public partial class App : System.Windows.Application
{
    // App sẽ tự start MainWindow theo StartupUri trong App.xaml
    // Nếu cần logic startup thêm (vd: check backend health), đặt ở đây
}
